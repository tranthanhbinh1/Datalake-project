# Kafka Connect worker configuration
bootstrap.servers=localhost:9092

# JDBC Source connector configuration
connector.class=io.confluent.connect.jdbc.JdbcSourceConnector
name=my-jdbc-connector
tasks.max=1
connection.url=jdbc:mysql://localhost:3306/mydatabase
connection.user=datalake_user
connection.password=240202

# FTPS Source connector - needs more fixings
connector.class=io.confluent.connect.ftps.FtpsSourceConnector
name=my-ftps-connector
kafka.topic=ftps-topic 
ftps.host='192.168.1.10'
fpts.username='bindz'
ftps.password='240202'
ftps.input.path=
ftps.finished.path=
